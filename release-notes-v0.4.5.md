xCalc v0.4.5 · 边距与默认缩放调整

## 更新

- 数字与蓝色线框、关联引线的边距从 1px 调整为 **3px**（世界坐标，随缩放）
- **默认打开时画布缩放为最小档**（0.35），首次使用视野更大；
  已有存档仍保留各自的缩放状态
- 说明：v0.4.4 已根治数字偏移问题（alphabetic 基线 + 墨迹居中）；
  本版本经像素级扫描验证，全缩放范围内数字与线框偏差均为亚像素级

安装：下载 APK 直接安装（debug 签名，Android 5.0+，零权限）。
